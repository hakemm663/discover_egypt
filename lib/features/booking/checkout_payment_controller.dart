@Deprecated(
  'Raw card data entry has been removed in favor of Stripe PaymentSheet. '
  'Keep this controller only for migration compatibility and remove after callers are cleaned up.',
)
class CheckoutPaymentController {
  const CheckoutPaymentController();
}
